#!/bin/bash

export PATH=$HOME/kubernetes/platforms/linux/amd64:$PATH

gcloud container clusters get-credentials buildkite-agent-fleet \
    --zone us-central1-a --project tardis-1

kubectl create -f container-engine.yaml
